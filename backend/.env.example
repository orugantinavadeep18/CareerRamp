# CareerRamp Backend — Environment Variables
# GDG Hackathon Solasta 2026 · IIITDM Kurnool
# Copy this file to .env and fill in your values

# ── Server ──
PORT=3001
NODE_ENV=development

# ── MongoDB Atlas ──
# Create a free cluster at: https://mongodb.com/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=mongodb+srv://your_user:your_password@your_cluster.mongodb.net/careerRamp

# ── JWT Auth ──
# Use any long random string (min 32 chars recommended)
JWT_SECRET=your_super_secret_jwt_key_here_min_32_chars

# ── Google Gemini API ──
# Get free keys at: https://aistudio.google.com/app/apikey (one key per Google account)
# Keys are tried in order: 1 → 2 → 3 → 4 on any error or quota limit (429)
# You can use the same key for all four if you only have one
GEMINI_API_KEY=your_primary_gemini_api_key_here
GEMINI_API_KEY_2=your_backup1_gemini_api_key_here
GEMINI_API_KEY_3=your_backup2_gemini_api_key_here
GEMINI_API_KEY_4=your_backup3_gemini_api_key_here

# ── CORS ──
FRONTEND_URL=http://localhost:5173
